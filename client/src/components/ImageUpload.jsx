// client/src/components/ImageUpload.jsx - PLACEHOLDER
const ImageUpload = ({ onUploadSuccess }) => {
  return (
    <div>
      <p>Image upload feature (coming soon)</p>
      <button onClick={() => onUploadSuccess('https://example.com/image.jpg')}>
        Use Sample Image
      </button>
    </div>
  );
};

export default ImageUpload;